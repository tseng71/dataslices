# Disappearing Stars v2 release

This release merge triggers the GitHub Pages build that expands the validated v2 source bundle, builds the Bright Star Catalog and Natural Earth data locally, validates the static site, saves the source files to the repository, and deploys GitHub Pages.
