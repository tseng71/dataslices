# QA notes

## 自动检查

- `npm run check`：Svelte diagnostics 为 0 errors / 0 warnings。
- `BASE_PATH=/dataslices/stories/lights-out npm run build`：SvelteKit 静态预渲染成功。
- 构建产物、图片、字体与公开方法文档存在性检查。
- `python scripts/audit_story.py stories/lights-out/build`。

## 手动检查矩阵

| 项目 | 320 px | 390 px | 768 px | 1440 px |
|---|---:|---:|---:|---:|
| 标题无横向溢出 | 通过断点审查 | 通过断点审查 | 通过断点审查 | 1348 px 实页通过 |
| 滑杆可触摸/键盘操作 | 通过原生控件审查 | 通过原生控件审查 | 通过原生控件审查 | 实页通过 |
| 100 格与步骤同步 | 通过状态与断点审查 | 通过状态与断点审查 | 通过状态与断点审查 | 实页通过 |
| 标签页鼠标与方向键 | 通过控件审查 | 通过控件审查 | 通过控件审查 | 实页通过 |
| 减少动画 | 通过 CSS 审查 | 通过 CSS 审查 | 通过 CSS 审查 | 通过 CSS 审查 |

远程浏览器没有提供可调整视口，320/390/768 为 CSS 断点、控件尺寸和源代码审查，不写成真实设备实测。

## 内容检查

- 所有数值可追溯至 `sources.md`。
- 未使用 GIBS 历史空影像计算恢复率。
- 未把 500 米像素解释为家庭或街道。
- 未把亮点直接认定为关键设施供电。
- 不跨事件比较不可比亮度。
- 100 格严格为 4 + 40 + 53 + 3，不写成家庭或真实街区个数。
- 首段、主视觉、城乡差异、证据边界与结尾共同服务同一论点。

## 上线验证

- GitHub Pages 工作流 `30371492925` 完成，结论为 `success`；发布提交 `cfc383f1`。
- 最终 URL：`https://tseng71.github.io/dataslices/stories/lights-out/`。
- 1348 × 936 实际视口下，文档宽度等于视口宽度，无横向溢出；四幅当前状态图像均完整加载。
- 滑杆从 52 调至 80 后，分界线与 clip-path 同步为 80%。
- 500 米/30 米切换后显示 289 个模型网格；城乡指标切换后显示 61%/39%。
- 加拉加斯标签点击成功，方向键向右切换到 Ian。
- Scrolly 实际到达 97/100 和 100/100，sticky 顶部位置正确。
- 页面自身无 JavaScript 控制台错误；观察到的错误仅来自浏览器扩展。
