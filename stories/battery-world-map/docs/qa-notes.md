# v1.0 QA 记录

日期：2026-07-28

## 自动检查

- `node --check`：电池、AI、城市热三篇故事的 JavaScript 通过。
- `python -m json.tool`：`data/materials.json` 通过。
- `audit_story.py stories/battery-world-map`：通过；未发现元话语，黏性叙事、互动、来源、降级与减弱动效信号齐全。
- 首页：4 张故事卡。
- 电池正文：约 2,300 个可见文字字符，17 个原生按钮，无横向溢出。
- AI 与城市热：互动脚本通过，无横向溢出。

## 浏览器操作

使用无头 Chromium 检查 1440×1000 桌面视口与 390×844 手机视口：

- 首页四个故事入口存在；
- 电池地图可从钴矿山切换到钴加工；
- LFP + 钴压力测试显示“不使用”，没有把份额写成零产量；
- `?chem=nmc&material=graphite&stage=process` 可恢复为 95% 加工暴露；
- AI 预算滑杆在 40 美元时显示 571M token；
- 城市热页面生成 356 个位置符号，100% 植被端点显示 76.6°F；
- 三个新/恢复页面在手机视口无横向溢出。

本地无头浏览器受运行环境限制使用 `file:` 载入，因此电池数据请求触发预期的文件协议警告并转入内置回退副本；静态 HTTP 请求单独确认 JSON 返回成功。GitHub Pages 使用 HTTPS，不触发该文件协议警告。

## 人工复核

- 矿山、储量、加工、贸易和规划产能没有混成同一序列。
- 压力测试标为静态暴露，不使用价格预测语言。
- 刚果（金）的国家份额没有被直接推导为所有矿山的劳工结论。
- 旧的《消失的星空》目录未修改。

